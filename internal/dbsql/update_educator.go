// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type UpdateEducatorParams struct {
	GivenName                string `json:"given_name"`
	ChosenName               string `json:"chosen_name"`
	FamilyName               string `json:"family_name"`
	Email                    string `json:"email"`
	Role                     string `json:"role"`
	LastEventCommitPosition  int64  `json:"last_event_commit_position"`
	LastEventPreparePosition int64  `json:"last_event_prepare_position"`
	UpdatedAt                string `json:"updated_at"`
	Id                       string `json:"id"`
}

type UpdateEducatorStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func UpdateEducator(tx *sqlite.Conn) *UpdateEducatorStmt {
	const querySQL = `
UPDATE educators
SET
	given_name = ?1,
	chosen_name = ?2,
	family_name = ?3,
	email = ?4,
	role = ?5,
	last_event_commit_position = ?6,
	last_event_prepare_position = ?7,
	updated_at = ?8
WHERE id = ?9
    `

	ps := &UpdateEducatorStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *UpdateEducatorStmt) Run(
	params UpdateEducatorParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, params.GivenName)

	bindIndex++
	stmt.BindText(bindIndex, params.ChosenName)

	bindIndex++
	stmt.BindText(bindIndex, params.FamilyName)

	bindIndex++
	stmt.BindText(bindIndex, params.Email)

	bindIndex++
	stmt.BindText(bindIndex, params.Role)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++
	stmt.BindText(bindIndex, params.UpdatedAt)

	bindIndex++
	stmt.BindText(bindIndex, params.Id)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute updateeducator SQL: %w", err)
	}

	return nil
}

func OnceUpdateEducator(
	tx *sqlite.Conn,
	params UpdateEducatorParams,
) (
	err error,
) {
	ps := UpdateEducator(tx)

	return ps.Run(
		params,
	)
}
