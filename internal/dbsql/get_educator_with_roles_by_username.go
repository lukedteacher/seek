// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type GetEducatorWithRolesByUsernameRes struct {
	Id         string  `json:"id"`
	GivenName  string  `json:"given_name"`
	ChosenName string  `json:"chosen_name"`
	FamilyName string  `json:"family_name"`
	Email      string  `json:"email"`
	Username   string  `json:"username"`
	CreatedAt  string  `json:"created_at"`
	UpdatedAt  string  `json:"updated_at"`
	Role       *string `json:"role"`
}

type GetEducatorWithRolesByUsernameStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func GetEducatorWithRolesByUsername(tx *sqlite.Conn) *GetEducatorWithRolesByUsernameStmt {
	const querySQL = `
SELECT
	e.id,
	e.given_name,
	e.chosen_name,
	e.family_name,
	e.email,
	e.username,
	e.created_at,
	e.updated_at,
	er.role
FROM educators e
LEFT JOIN educator_roles er ON e.id = er.educator_id AND er.archived_at IS NULL
WHERE e.archived_at IS NULL AND e.username = ?1
ORDER BY er.role
    `

	ps := &GetEducatorWithRolesByUsernameStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *GetEducatorWithRolesByUsernameStmt) Run(
	username string,
) (
	res []GetEducatorWithRolesByUsernameRes,
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, username)

	bindIndex++

	// Execute the query
	for {
		if hasRow, err := stmt.Step(); err != nil {
			return res, fmt.Errorf("failed to execute {{.Name.Lower}} SQL: %w", err)
		} else if !hasRow {
			break
		}

		row := GetEducatorWithRolesByUsernameRes{}
		row.Id = stmt.ColumnText(0)
		row.GivenName = stmt.ColumnText(1)
		row.ChosenName = stmt.ColumnText(2)
		row.FamilyName = stmt.ColumnText(3)
		row.Email = stmt.ColumnText(4)
		row.Username = stmt.ColumnText(5)
		row.CreatedAt = stmt.ColumnText(6)
		row.UpdatedAt = stmt.ColumnText(7)
		isNullRole := stmt.ColumnIsNull(8)
		if !isNullRole {
			tmp := stmt.ColumnText(8)
			row.Role = &tmp
		}
		res = append(res, row)
	}

	return res, nil
}

func OnceGetEducatorWithRolesByUsername(
	tx *sqlite.Conn,
	username string,
) (
	res []GetEducatorWithRolesByUsernameRes,
	err error,
) {
	ps := GetEducatorWithRolesByUsername(tx)

	return ps.Run(
		username,
	)
}
