// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type ListEducatorsByRoleRes struct {
	Id         string `json:"id"`
	GivenName  string `json:"given_name"`
	ChosenName string `json:"chosen_name"`
	FamilyName string `json:"family_name"`
	Email      string `json:"email"`
	Username   string `json:"username"`
	CreatedAt  string `json:"created_at"`
	UpdatedAt  string `json:"updated_at"`
	Role       string `json:"role"`
}

type ListEducatorsByRoleStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func ListEducatorsByRole(tx *sqlite.Conn) *ListEducatorsByRoleStmt {
	const querySQL = `
SELECT
    e.id,
    e.given_name,
    e.chosen_name,
    e.family_name,
    e.email,
    e.username,
    e.created_at,
    e.updated_at,
    er.role
FROM educators e
INNER JOIN educator_roles er ON e.id = er.educator_id AND er.archived_at IS NULL
WHERE e.archived_at IS NULL
  AND er.role = ?1
ORDER BY e.family_name DESC, e.given_name DESC, er.role ASC
    `

	ps := &ListEducatorsByRoleStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *ListEducatorsByRoleStmt) Run(
	role string,
) (
	res []ListEducatorsByRoleRes,
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, role)

	bindIndex++

	// Execute the query
	for {
		if hasRow, err := stmt.Step(); err != nil {
			return res, fmt.Errorf("failed to execute {{.Name.Lower}} SQL: %w", err)
		} else if !hasRow {
			break
		}

		row := ListEducatorsByRoleRes{}
		row.Id = stmt.ColumnText(0)
		row.GivenName = stmt.ColumnText(1)
		row.ChosenName = stmt.ColumnText(2)
		row.FamilyName = stmt.ColumnText(3)
		row.Email = stmt.ColumnText(4)
		row.Username = stmt.ColumnText(5)
		row.CreatedAt = stmt.ColumnText(6)
		row.UpdatedAt = stmt.ColumnText(7)
		row.Role = stmt.ColumnText(8)
		res = append(res, row)
	}

	return res, nil
}

func OnceListEducatorsByRole(
	tx *sqlite.Conn,
	role string,
) (
	res []ListEducatorsByRoleRes,
	err error,
) {
	ps := ListEducatorsByRole(tx)

	return ps.Run(
		role,
	)
}
