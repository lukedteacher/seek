// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type DeleteTeacherParams struct {
	DeletedAt                *string `json:"deleted_at"`
	LastEventCommitPosition  int64   `json:"last_event_commit_position"`
	LastEventPreparePosition int64   `json:"last_event_prepare_position"`
	Id                       string  `json:"id"`
}

type DeleteTeacherStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func DeleteTeacher(tx *sqlite.Conn) *DeleteTeacherStmt {
	const querySQL = `
UPDATE teachers
SET deleted_at = ?1,
	updated_at = ?1,
	last_event_commit_position = ?2,
	last_event_prepare_position = ?3
WHERE id = ?4
    `

	ps := &DeleteTeacherStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *DeleteTeacherStmt) Run(
	params DeleteTeacherParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	if params.DeletedAt == nil {
		stmt.BindNull(bindIndex)
	} else {
		stmt.BindText(bindIndex, *params.DeletedAt)

	}
	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++
	stmt.BindText(bindIndex, params.Id)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute deleteteacher SQL: %w", err)
	}

	return nil
}

func OnceDeleteTeacher(
	tx *sqlite.Conn,
	params DeleteTeacherParams,
) (
	err error,
) {
	ps := DeleteTeacher(tx)

	return ps.Run(
		params,
	)
}
