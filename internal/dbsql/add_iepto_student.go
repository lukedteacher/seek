// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type AddIeptoStudentParams struct {
	Id                       string `json:"id"`
	StudentId                string `json:"student_id"`
	StartDate                string `json:"start_date"`
	EndDate                  string `json:"end_date"`
	AmendedDate              string `json:"amended_date"`
	LastEventCommitPosition  int64  `json:"last_event_commit_position"`
	LastEventPreparePosition int64  `json:"last_event_prepare_position"`
	CreatedAt                string `json:"created_at"`
}

type AddIeptoStudentStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func AddIeptoStudent(tx *sqlite.Conn) *AddIeptoStudentStmt {
	const querySQL = `
INSERT INTO student_ieps (
	id, 
	student_id,
	start_date,
	end_date,
	amended_date,
	last_event_commit_position,
	last_event_prepare_position,
	created_at, 
	updated_at
)
VALUES (
	?1, 
	?2,
	?3,
	?4,
	?5,
	?6, 
	?7,
	?8, 
	?8
)
ON CONFLICT (id) DO NOTHING
    `

	ps := &AddIeptoStudentStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *AddIeptoStudentStmt) Run(
	params AddIeptoStudentParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, params.Id)

	bindIndex++
	stmt.BindText(bindIndex, params.StudentId)

	bindIndex++
	stmt.BindText(bindIndex, params.StartDate)

	bindIndex++
	stmt.BindText(bindIndex, params.EndDate)

	bindIndex++
	stmt.BindText(bindIndex, params.AmendedDate)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++
	stmt.BindText(bindIndex, params.CreatedAt)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute addieptostudent SQL: %w", err)
	}

	return nil
}

func OnceAddIeptoStudent(
	tx *sqlite.Conn,
	params AddIeptoStudentParams,
) (
	err error,
) {
	ps := AddIeptoStudent(tx)

	return ps.Run(
		params,
	)
}
