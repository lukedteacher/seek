// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type UpdateTeacherParams struct {
	FirstName                string  `json:"first_name"`
	ChosenName               *string `json:"chosen_name"`
	LastName                 string  `json:"last_name"`
	UpdatedAt                string  `json:"updated_at"`
	LastEventCommitPosition  int64   `json:"last_event_commit_position"`
	LastEventPreparePosition int64   `json:"last_event_prepare_position"`
	Id                       string  `json:"id"`
}

type UpdateTeacherStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func UpdateTeacher(tx *sqlite.Conn) *UpdateTeacherStmt {
	const querySQL = `
UPDATE teachers
SET first_name = ?1,
	chosen_name = ?2,
	last_name = ?3,
	updated_at = ?4,
	last_event_commit_position = ?5,
	last_event_prepare_position = ?6
WHERE id = ?7
    `

	ps := &UpdateTeacherStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *UpdateTeacherStmt) Run(
	params UpdateTeacherParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, params.FirstName)

	bindIndex++
	if params.ChosenName == nil {
		stmt.BindNull(bindIndex)
	} else {
		stmt.BindText(bindIndex, *params.ChosenName)

	}
	bindIndex++
	stmt.BindText(bindIndex, params.LastName)

	bindIndex++
	stmt.BindText(bindIndex, params.UpdatedAt)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++
	stmt.BindText(bindIndex, params.Id)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute updateteacher SQL: %w", err)
	}

	return nil
}

func OnceUpdateTeacher(
	tx *sqlite.Conn,
	params UpdateTeacherParams,
) (
	err error,
) {
	ps := UpdateTeacher(tx)

	return ps.Run(
		params,
	)
}
