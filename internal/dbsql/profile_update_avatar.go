// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type ProfileUpdateAvatarParams struct {
	Avatar                   *string `json:"avatar"`
	LastEventCommitPosition  int64   `json:"last_event_commit_position"`
	LastEventPreparePosition int64   `json:"last_event_prepare_position"`
	UpdatedAt                string  `json:"updated_at"`
	UserId                   string  `json:"user_id"`
}

type ProfileUpdateAvatarStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func ProfileUpdateAvatar(tx *sqlite.Conn) *ProfileUpdateAvatarStmt {
	const querySQL = `
UPDATE profile_stats 
 SET 
	avatar = ?1, 
	last_event_commit_position = ?2,
	last_event_prepare_position = ?3,
	updated_at = ?4
WHERE user_id = ?5
    `

	ps := &ProfileUpdateAvatarStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *ProfileUpdateAvatarStmt) Run(
	params ProfileUpdateAvatarParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	if params.Avatar == nil {
		stmt.BindNull(bindIndex)
	} else {
		stmt.BindText(bindIndex, *params.Avatar)

	}
	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++
	stmt.BindText(bindIndex, params.UpdatedAt)

	bindIndex++
	stmt.BindText(bindIndex, params.UserId)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute profileupdateavatar SQL: %w", err)
	}

	return nil
}

func OnceProfileUpdateAvatar(
	tx *sqlite.Conn,
	params ProfileUpdateAvatarParams,
) (
	err error,
) {
	ps := ProfileUpdateAvatar(tx)

	return ps.Run(
		params,
	)
}
