// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type CreateProfileForUserParams struct {
	UserId                   string `json:"user_id"`
	LastEventCommitPosition  int64  `json:"last_event_commit_position"`
	LastEventPreparePosition int64  `json:"last_event_prepare_position"`
}

type CreateProfileForUserStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func CreateProfileForUser(tx *sqlite.Conn) *CreateProfileForUserStmt {
	const querySQL = `
INSERT INTO profile_stats (
	user_id, 
	last_event_commit_position, 
	last_event_prepare_position
)
VALUES (
	?1, 
	?2, 
	?3
)
ON CONFLICT (user_id) DO NOTHING
    `

	ps := &CreateProfileForUserStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *CreateProfileForUserStmt) Run(
	params CreateProfileForUserParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, params.UserId)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute createprofileforuser SQL: %w", err)
	}

	return nil
}

func OnceCreateProfileForUser(
	tx *sqlite.Conn,
	params CreateProfileForUserParams,
) (
	err error,
) {
	ps := CreateProfileForUser(tx)

	return ps.Run(
		params,
	)
}
