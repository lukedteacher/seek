// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type RemoveStudentBookmarkParams struct {
	UserId    string `json:"user_id"`
	StudentId string `json:"student_id"`
}

type RemoveStudentBookmarkStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func RemoveStudentBookmark(tx *sqlite.Conn) *RemoveStudentBookmarkStmt {
	const querySQL = `
DELETE FROM user_student_bookmarks
WHERE user_id = ?1 
	AND student_id = ?2
    `

	ps := &RemoveStudentBookmarkStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *RemoveStudentBookmarkStmt) Run(
	params RemoveStudentBookmarkParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, params.UserId)

	bindIndex++
	stmt.BindText(bindIndex, params.StudentId)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute removestudentbookmark SQL: %w", err)
	}

	return nil
}

func OnceRemoveStudentBookmark(
	tx *sqlite.Conn,
	params RemoveStudentBookmarkParams,
) (
	err error,
) {
	ps := RemoveStudentBookmark(tx)

	return ps.Run(
		params,
	)
}
