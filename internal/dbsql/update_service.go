// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type UpdateServiceParams struct {
	ServiceName              string `json:"service_name"`
	ServiceType              string `json:"service_type"`
	IndirectMinutes          int64  `json:"indirect_minutes"`
	DirectMinutes            int64  `json:"direct_minutes"`
	FrequencyCount           int64  `json:"frequency_count"`
	FrequencyType            string `json:"frequency_type"`
	LocationId               string `json:"location_id"`
	StartDate                string `json:"start_date"`
	EndDate                  string `json:"end_date"`
	ProviderId               string `json:"provider_id"`
	LastEventCommitPosition  int64  `json:"last_event_commit_position"`
	LastEventPreparePosition int64  `json:"last_event_prepare_position"`
	UpdatedAt                string `json:"updated_at"`
	Id                       string `json:"id"`
}

type UpdateServiceStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func UpdateService(tx *sqlite.Conn) *UpdateServiceStmt {
	const querySQL = `
UPDATE iep_services
SET
	service_name = ?1,
	service_type = ?2,
	indirect_minutes = ?3,
	direct_minutes = ?4,
	frequency_count = ?5,
	frequency_type = ?6,
	location_id = ?7,
	start_date = ?8,
	end_date = ?9,
	provider_id = ?10,
	last_event_commit_position = ?11,
	last_event_prepare_position = ?12,
	updated_at = ?13
WHERE id = ?14
    `

	ps := &UpdateServiceStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *UpdateServiceStmt) Run(
	params UpdateServiceParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, params.ServiceName)

	bindIndex++
	stmt.BindText(bindIndex, params.ServiceType)

	bindIndex++
	stmt.BindInt64(bindIndex, params.IndirectMinutes)

	bindIndex++
	stmt.BindInt64(bindIndex, params.DirectMinutes)

	bindIndex++
	stmt.BindInt64(bindIndex, params.FrequencyCount)

	bindIndex++
	stmt.BindText(bindIndex, params.FrequencyType)

	bindIndex++
	stmt.BindText(bindIndex, params.LocationId)

	bindIndex++
	stmt.BindText(bindIndex, params.StartDate)

	bindIndex++
	stmt.BindText(bindIndex, params.EndDate)

	bindIndex++
	stmt.BindText(bindIndex, params.ProviderId)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++
	stmt.BindText(bindIndex, params.UpdatedAt)

	bindIndex++
	stmt.BindText(bindIndex, params.Id)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute updateservice SQL: %w", err)
	}

	return nil
}

func OnceUpdateService(
	tx *sqlite.Conn,
	params UpdateServiceParams,
) (
	err error,
) {
	ps := UpdateService(tx)

	return ps.Run(
		params,
	)
}
