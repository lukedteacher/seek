// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type ListPeriodsForEducatorRes struct {
	Id          string `json:"id"`
	Title       string `json:"title"`
	ServiceType string `json:"service_type"`
	StartTime   string `json:"start_time"`
	Duration    int64  `json:"duration"`
	DaysBitmask int64  `json:"days_bitmask"`
	CreatedAt   string `json:"created_at"`
	UpdatedAt   string `json:"updated_at"`
}

type ListPeriodsForEducatorStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func ListPeriodsForEducator(tx *sqlite.Conn) *ListPeriodsForEducatorStmt {
	const querySQL = `
SELECT
	periods.id, 
	periods.title, 
	periods.service_type,
	periods.start_time, 
	periods.duration, 
	periods.days_bitmask, 
	periods.created_at, 
	periods.updated_at
FROM periods
JOIN educators_periods ON periods.id = educators_periods.period_id
WHERE educators_periods.educator_id = ?1
ORDER BY start_time DESC
    `

	ps := &ListPeriodsForEducatorStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *ListPeriodsForEducatorStmt) Run(
	educatorId string,
) (
	res []ListPeriodsForEducatorRes,
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, educatorId)

	bindIndex++

	// Execute the query
	for {
		if hasRow, err := stmt.Step(); err != nil {
			return res, fmt.Errorf("failed to execute {{.Name.Lower}} SQL: %w", err)
		} else if !hasRow {
			break
		}

		row := ListPeriodsForEducatorRes{}
		row.Id = stmt.ColumnText(0)
		row.Title = stmt.ColumnText(1)
		row.ServiceType = stmt.ColumnText(2)
		row.StartTime = stmt.ColumnText(3)
		row.Duration = stmt.ColumnInt64(4)
		row.DaysBitmask = stmt.ColumnInt64(5)
		row.CreatedAt = stmt.ColumnText(6)
		row.UpdatedAt = stmt.ColumnText(7)
		res = append(res, row)
	}

	return res, nil
}

func OnceListPeriodsForEducator(
	tx *sqlite.Conn,
	educatorId string,
) (
	res []ListPeriodsForEducatorRes,
	err error,
) {
	ps := ListPeriodsForEducator(tx)

	return ps.Run(
		educatorId,
	)
}
