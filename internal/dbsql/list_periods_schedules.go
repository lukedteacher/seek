// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type ListPeriodsSchedulesRes struct {
	PeriodId   string `json:"period_id"`
	ScheduleId string `json:"schedule_id"`
}

type ListPeriodsSchedulesStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func ListPeriodsSchedules(tx *sqlite.Conn) *ListPeriodsSchedulesStmt {
	const querySQL = `
SELECT period_id, schedule_id
FROM periods_schedules
    `

	ps := &ListPeriodsSchedulesStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *ListPeriodsSchedulesStmt) Run() (
	res []ListPeriodsSchedulesRes,
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	// Execute the query
	for {
		if hasRow, err := stmt.Step(); err != nil {
			return res, fmt.Errorf("failed to execute {{.Name.Lower}} SQL: %w", err)
		} else if !hasRow {
			break
		}

		row := ListPeriodsSchedulesRes{}
		row.PeriodId = stmt.ColumnText(0)
		row.ScheduleId = stmt.ColumnText(1)
		res = append(res, row)
	}

	return res, nil
}

func OnceListPeriodsSchedules(
	tx *sqlite.Conn,
) (
	res []ListPeriodsSchedulesRes,
	err error,
) {
	ps := ListPeriodsSchedules(tx)

	return ps.Run()
}
