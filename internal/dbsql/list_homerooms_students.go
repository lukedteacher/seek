// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type ListHomeroomsStudentsRes struct {
	HomeroomId string `json:"homeroom_id"`
	StudentId  string `json:"student_id"`
}

type ListHomeroomsStudentsStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func ListHomeroomsStudents(tx *sqlite.Conn) *ListHomeroomsStudentsStmt {
	const querySQL = `
SELECT homeroom_id, student_id
FROM homerooms_students
    `

	ps := &ListHomeroomsStudentsStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *ListHomeroomsStudentsStmt) Run() (
	res []ListHomeroomsStudentsRes,
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	// Execute the query
	for {
		if hasRow, err := stmt.Step(); err != nil {
			return res, fmt.Errorf("failed to execute {{.Name.Lower}} SQL: %w", err)
		} else if !hasRow {
			break
		}

		row := ListHomeroomsStudentsRes{}
		row.HomeroomId = stmt.ColumnText(0)
		row.StudentId = stmt.ColumnText(1)
		res = append(res, row)
	}

	return res, nil
}

func OnceListHomeroomsStudents(
	tx *sqlite.Conn,
) (
	res []ListHomeroomsStudentsRes,
	err error,
) {
	ps := ListHomeroomsStudents(tx)

	return ps.Run()
}
