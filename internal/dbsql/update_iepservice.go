// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type UpdateIepserviceParams struct {
	StudentId                string `json:"student_id"`
	ServiceName              string `json:"service_name"`
	ServiceType              string `json:"service_type"`
	IndirectMinutes          int64  `json:"indirect_minutes"`
	DirectMinutes            int64  `json:"direct_minutes"`
	FrequencyCount           int64  `json:"frequency_count"`
	FrequencyType            string `json:"frequency_type"`
	Location                 string `json:"location"`
	StartDate                string `json:"start_date"`
	EndDate                  string `json:"end_date"`
	Provider                 string `json:"provider"`
	LastEventCommitPosition  int64  `json:"last_event_commit_position"`
	LastEventPreparePosition int64  `json:"last_event_prepare_position"`
	UpdatedAt                string `json:"updated_at"`
	Id                       string `json:"id"`
}

type UpdateIepserviceStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func UpdateIepservice(tx *sqlite.Conn) *UpdateIepserviceStmt {
	const querySQL = `
UPDATE iep_services
SET
	student_id = ?1,
	service_name = ?2,
	service_type = ?3,
	indirect_minutes = ?4,
	direct_minutes = ?5,
	frequency_count = ?6,
	frequency_type = ?7,
	location = ?8,
	start_date = ?9,
	end_date = ?10,
	provider = ?11,
	last_event_commit_position = ?12,
	last_event_prepare_position = ?13,
	updated_at = ?14
WHERE id = ?15
    `

	ps := &UpdateIepserviceStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *UpdateIepserviceStmt) Run(
	params UpdateIepserviceParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, params.StudentId)

	bindIndex++
	stmt.BindText(bindIndex, params.ServiceName)

	bindIndex++
	stmt.BindText(bindIndex, params.ServiceType)

	bindIndex++
	stmt.BindInt64(bindIndex, params.IndirectMinutes)

	bindIndex++
	stmt.BindInt64(bindIndex, params.DirectMinutes)

	bindIndex++
	stmt.BindInt64(bindIndex, params.FrequencyCount)

	bindIndex++
	stmt.BindText(bindIndex, params.FrequencyType)

	bindIndex++
	stmt.BindText(bindIndex, params.Location)

	bindIndex++
	stmt.BindText(bindIndex, params.StartDate)

	bindIndex++
	stmt.BindText(bindIndex, params.EndDate)

	bindIndex++
	stmt.BindText(bindIndex, params.Provider)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++
	stmt.BindText(bindIndex, params.UpdatedAt)

	bindIndex++
	stmt.BindText(bindIndex, params.Id)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute updateiepservice SQL: %w", err)
	}

	return nil
}

func OnceUpdateIepservice(
	tx *sqlite.Conn,
	params UpdateIepserviceParams,
) (
	err error,
) {
	ps := UpdateIepservice(tx)

	return ps.Run(
		params,
	)
}
