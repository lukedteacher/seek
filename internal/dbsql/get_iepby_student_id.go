// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type GetIepbyStudentIdRes struct {
	Id            string `json:"id"`
	MarssId       string `json:"marss_id"`
	GivenName     string `json:"given_name"`
	ChosenName    string `json:"chosen_name"`
	FamilyName    string `json:"family_name"`
	Email         string `json:"email"`
	Username      string `json:"username"`
	Grade         int64  `json:"grade"`
	HomeroomId    string `json:"homeroom_id"`
	PlanType      int64  `json:"plan_type"`
	CreatedAt     string `json:"created_at"`
	UpdatedAt     string `json:"updated_at"`
	IepId         string `json:"iep_id"`
	CaseManagerId string `json:"case_manager_id"`
	StartDate     string `json:"start_date"`
	EndDate       string `json:"end_date"`
	AmendedDate   string `json:"amended_date"`
	IepCreatedAt  string `json:"iep_created_at"`
	IepUpdatedAt  string `json:"iep_updated_at"`
}

type GetIepbyStudentIdStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func GetIepbyStudentId(tx *sqlite.Conn) *GetIepbyStudentIdStmt {
	const querySQL = `
SELECT 
	s.id, 
	s.marss_id,
	s.given_name, 
	s.chosen_name, 
	s.family_name, 
	s.email,
	s.username,
	s.grade, 
	s.homeroom_id, 
	s.plan_type,
	s.created_at, 
	s.updated_at,
	si.id AS iep_id,
	si.case_manager_id, 
	si.start_date,
	si.end_date,
	si.amended_date,
	si.created_at AS iep_created_at,
	si.updated_at AS iep_updated_at
FROM student_ieps si
INNER JOIN students s ON si.student_id = s.id
WHERE s.archived_at IS NULL
	AND si.archived_at IS NULL
	AND si.student_id = ?1
    `

	ps := &GetIepbyStudentIdStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *GetIepbyStudentIdStmt) Run(
	id string,
) (
	res *GetIepbyStudentIdRes,
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, id)

	bindIndex++

	// Execute the query
	if hasRow, err := stmt.Step(); err != nil {
		return res, fmt.Errorf("failed to execute {{.Name.Lower}} SQL: %w", err)
	} else if hasRow {
		row := GetIepbyStudentIdRes{}
		row.Id = stmt.ColumnText(0)
		row.MarssId = stmt.ColumnText(1)
		row.GivenName = stmt.ColumnText(2)
		row.ChosenName = stmt.ColumnText(3)
		row.FamilyName = stmt.ColumnText(4)
		row.Email = stmt.ColumnText(5)
		row.Username = stmt.ColumnText(6)
		row.Grade = stmt.ColumnInt64(7)
		row.HomeroomId = stmt.ColumnText(8)
		row.PlanType = stmt.ColumnInt64(9)
		row.CreatedAt = stmt.ColumnText(10)
		row.UpdatedAt = stmt.ColumnText(11)
		row.IepId = stmt.ColumnText(12)
		row.CaseManagerId = stmt.ColumnText(13)
		row.StartDate = stmt.ColumnText(14)
		row.EndDate = stmt.ColumnText(15)
		row.AmendedDate = stmt.ColumnText(16)
		row.IepCreatedAt = stmt.ColumnText(17)
		row.IepUpdatedAt = stmt.ColumnText(18)
		res = &row
	}

	return res, nil
}

func OnceGetIepbyStudentId(
	tx *sqlite.Conn,
	id string,
) (
	res *GetIepbyStudentIdRes,
	err error,
) {
	ps := GetIepbyStudentId(tx)

	return ps.Run(
		id,
	)
}
