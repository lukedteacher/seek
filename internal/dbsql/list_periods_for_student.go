// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type ListPeriodsForStudentRes struct {
	Id          string `json:"id"`
	Title       string `json:"title"`
	ServiceType string `json:"service_type"`
	StartTime   string `json:"start_time"`
	Duration    int64  `json:"duration"`
	DaysBitmask int64  `json:"days_bitmask"`
	CreatedAt   string `json:"created_at"`
	UpdatedAt   string `json:"updated_at"`
}

type ListPeriodsForStudentStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func ListPeriodsForStudent(tx *sqlite.Conn) *ListPeriodsForStudentStmt {
	const querySQL = `
SELECT
	periods.id, 
	periods.title, 
	periods.service_type,
	periods.start_time, 
	periods.duration, 
	periods.days_bitmask, 
	periods.created_at, 
	periods.updated_at
FROM periods
JOIN periods_students ON periods.id = periods_students.period_id
WHERE periods_students.student_id = ?1
ORDER BY start_time DESC
    `

	ps := &ListPeriodsForStudentStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *ListPeriodsForStudentStmt) Run(
	studentId string,
) (
	res []ListPeriodsForStudentRes,
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, studentId)

	bindIndex++

	// Execute the query
	for {
		if hasRow, err := stmt.Step(); err != nil {
			return res, fmt.Errorf("failed to execute {{.Name.Lower}} SQL: %w", err)
		} else if !hasRow {
			break
		}

		row := ListPeriodsForStudentRes{}
		row.Id = stmt.ColumnText(0)
		row.Title = stmt.ColumnText(1)
		row.ServiceType = stmt.ColumnText(2)
		row.StartTime = stmt.ColumnText(3)
		row.Duration = stmt.ColumnInt64(4)
		row.DaysBitmask = stmt.ColumnInt64(5)
		row.CreatedAt = stmt.ColumnText(6)
		row.UpdatedAt = stmt.ColumnText(7)
		res = append(res, row)
	}

	return res, nil
}

func OnceListPeriodsForStudent(
	tx *sqlite.Conn,
	studentId string,
) (
	res []ListPeriodsForStudentRes,
	err error,
) {
	ps := ListPeriodsForStudent(tx)

	return ps.Run(
		studentId,
	)
}
