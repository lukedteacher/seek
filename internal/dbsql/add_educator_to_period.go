// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type AddEducatorToPeriodParams struct {
	PeriodId                 string `json:"period_id"`
	EducatorId               string `json:"educator_id"`
	LastEventCommitPosition  int64  `json:"last_event_commit_position"`
	LastEventPreparePosition int64  `json:"last_event_prepare_position"`
	CreatedAt                string `json:"created_at"`
}

type AddEducatorToPeriodStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func AddEducatorToPeriod(tx *sqlite.Conn) *AddEducatorToPeriodStmt {
	const querySQL = `
INSERT INTO educators_periods (
	period_id, 
	educator_id, 
	last_event_commit_position, 
	last_event_prepare_position,
	created_at, 
	updated_at
)
VALUES (
	?1, 
	?2, 
	?3, 
	?4,
	?5, 
	?5
)
ON CONFLICT (period_id, educator_id) DO NOTHING
    `

	ps := &AddEducatorToPeriodStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *AddEducatorToPeriodStmt) Run(
	params AddEducatorToPeriodParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, params.PeriodId)

	bindIndex++
	stmt.BindText(bindIndex, params.EducatorId)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++
	stmt.BindText(bindIndex, params.CreatedAt)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute addeducatortoperiod SQL: %w", err)
	}

	return nil
}

func OnceAddEducatorToPeriod(
	tx *sqlite.Conn,
	params AddEducatorToPeriodParams,
) (
	err error,
) {
	ps := AddEducatorToPeriod(tx)

	return ps.Run(
		params,
	)
}
