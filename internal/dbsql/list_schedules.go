// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type ListSchedulesRes struct {
	Id        string `json:"id"`
	Title     string `json:"title"`
	TeacherId string `json:"teacher_id"`
	CreatedAt string `json:"created_at"`
	UpdatedAt string `json:"updated_at"`
}

type ListSchedulesStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func ListSchedules(tx *sqlite.Conn) *ListSchedulesStmt {
	const querySQL = `
SELECT id, title, teacher_id, created_at, updated_at
FROM schedules
WHERE deleted_at IS NULL
ORDER BY title DESC, id DESC
    `

	ps := &ListSchedulesStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *ListSchedulesStmt) Run() (
	res []ListSchedulesRes,
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	// Execute the query
	for {
		if hasRow, err := stmt.Step(); err != nil {
			return res, fmt.Errorf("failed to execute {{.Name.Lower}} SQL: %w", err)
		} else if !hasRow {
			break
		}

		row := ListSchedulesRes{}
		row.Id = stmt.ColumnText(0)
		row.Title = stmt.ColumnText(1)
		row.TeacherId = stmt.ColumnText(2)
		row.CreatedAt = stmt.ColumnText(3)
		row.UpdatedAt = stmt.ColumnText(4)
		res = append(res, row)
	}

	return res, nil
}

func OnceListSchedules(
	tx *sqlite.Conn,
) (
	res []ListSchedulesRes,
	err error,
) {
	ps := ListSchedules(tx)

	return ps.Run()
}
