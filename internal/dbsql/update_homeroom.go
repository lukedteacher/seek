// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type UpdateHomeroomParams struct {
	Title                    string `json:"title"`
	GradesBitmask            int64  `json:"grades_bitmask"`
	LocationId               string `json:"location_id"`
	Image                    string `json:"image"`
	LastEventCommitPosition  int64  `json:"last_event_commit_position"`
	LastEventPreparePosition int64  `json:"last_event_prepare_position"`
	UpdatedAt                string `json:"updated_at"`
	Id                       string `json:"id"`
}

type UpdateHomeroomStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func UpdateHomeroom(tx *sqlite.Conn) *UpdateHomeroomStmt {
	const querySQL = `
UPDATE homerooms
SET
	title = ?1,
	grades_bitmask = ?2,
	location_id = ?3,
	image = ?4,
	last_event_commit_position = ?5,
	last_event_prepare_position = ?6,
	updated_at = ?7
WHERE id = ?8
    `

	ps := &UpdateHomeroomStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *UpdateHomeroomStmt) Run(
	params UpdateHomeroomParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, params.Title)

	bindIndex++
	stmt.BindInt64(bindIndex, params.GradesBitmask)

	bindIndex++
	stmt.BindText(bindIndex, params.LocationId)

	bindIndex++
	stmt.BindText(bindIndex, params.Image)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++
	stmt.BindText(bindIndex, params.UpdatedAt)

	bindIndex++
	stmt.BindText(bindIndex, params.Id)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute updatehomeroom SQL: %w", err)
	}

	return nil
}

func OnceUpdateHomeroom(
	tx *sqlite.Conn,
	params UpdateHomeroomParams,
) (
	err error,
) {
	ps := UpdateHomeroom(tx)

	return ps.Run(
		params,
	)
}
