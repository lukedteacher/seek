// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type GetIepserviceRes struct {
	Id              string `json:"id"`
	StudentId       string `json:"student_id"`
	ServiceName     string `json:"service_name"`
	ServiceType     string `json:"service_type"`
	IndirectMinutes int64  `json:"indirect_minutes"`
	DirectMinutes   int64  `json:"direct_minutes"`
	FrequencyCount  int64  `json:"frequency_count"`
	FrequencyType   string `json:"frequency_type"`
	Location        string `json:"location"`
	StartDate       string `json:"start_date"`
	EndDate         string `json:"end_date"`
	Provider        string `json:"provider"`
	CreatedAt       string `json:"created_at"`
	UpdatedAt       string `json:"updated_at"`
}

type GetIepserviceStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func GetIepservice(tx *sqlite.Conn) *GetIepserviceStmt {
	const querySQL = `
SELECT 
	id, 
	student_id, 
	service_name,
	service_type, 
	indirect_minutes,
	direct_minutes,
	frequency_count,
	frequency_type,
	location,
	start_date,
	end_date,
	provider,
	created_at, 
	updated_at
FROM iep_services
WHERE archived_at IS NULL
	AND id = ?1
    `

	ps := &GetIepserviceStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *GetIepserviceStmt) Run(
	id string,
) (
	res *GetIepserviceRes,
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, id)

	bindIndex++

	// Execute the query
	if hasRow, err := stmt.Step(); err != nil {
		return res, fmt.Errorf("failed to execute {{.Name.Lower}} SQL: %w", err)
	} else if hasRow {
		row := GetIepserviceRes{}
		row.Id = stmt.ColumnText(0)
		row.StudentId = stmt.ColumnText(1)
		row.ServiceName = stmt.ColumnText(2)
		row.ServiceType = stmt.ColumnText(3)
		row.IndirectMinutes = stmt.ColumnInt64(4)
		row.DirectMinutes = stmt.ColumnInt64(5)
		row.FrequencyCount = stmt.ColumnInt64(6)
		row.FrequencyType = stmt.ColumnText(7)
		row.Location = stmt.ColumnText(8)
		row.StartDate = stmt.ColumnText(9)
		row.EndDate = stmt.ColumnText(10)
		row.Provider = stmt.ColumnText(11)
		row.CreatedAt = stmt.ColumnText(12)
		row.UpdatedAt = stmt.ColumnText(13)
		res = &row
	}

	return res, nil
}

func OnceGetIepservice(
	tx *sqlite.Conn,
	id string,
) (
	res *GetIepserviceRes,
	err error,
) {
	ps := GetIepservice(tx)

	return ps.Run(
		id,
	)
}
