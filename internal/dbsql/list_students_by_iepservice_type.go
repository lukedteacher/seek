// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type ListStudentsByIepserviceTypeRes struct {
	Id          string  `json:"id"`
	MarssId     string  `json:"marss_id"`
	GivenName   string  `json:"given_name"`
	ChosenName  string  `json:"chosen_name"`
	FamilyName  string  `json:"family_name"`
	Email       string  `json:"email"`
	Username    string  `json:"username"`
	Grade       int64   `json:"grade"`
	Homeroom    string  `json:"homeroom"`
	CaseManager string  `json:"case_manager"`
	CreatedAt   string  `json:"created_at"`
	UpdatedAt   string  `json:"updated_at"`
	ArchivedAt  *string `json:"archived_at"`
}

type ListStudentsByIepserviceTypeStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func ListStudentsByIepserviceType(tx *sqlite.Conn) *ListStudentsByIepserviceTypeStmt {
	const querySQL = `
SELECT 
    s.id, 
		s.marss_id,
    s.given_name, 
    s.chosen_name, 
    s.family_name, 
		s.email,
		s.username,
    s.grade, 
    s.homeroom, 
    s.case_manager, 
    s.created_at, 
    s.updated_at,
    s.archived_at
FROM students s
WHERE EXISTS (
    SELECT 1 FROM iep_services ieps
    WHERE ieps.student_id = s.id
      AND ieps.service_type = ?1
      AND ieps.archived_at IS NULL
)
    `

	ps := &ListStudentsByIepserviceTypeStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *ListStudentsByIepserviceTypeStmt) Run(
	serviceType string,
) (
	res []ListStudentsByIepserviceTypeRes,
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, serviceType)

	bindIndex++

	// Execute the query
	for {
		if hasRow, err := stmt.Step(); err != nil {
			return res, fmt.Errorf("failed to execute {{.Name.Lower}} SQL: %w", err)
		} else if !hasRow {
			break
		}

		row := ListStudentsByIepserviceTypeRes{}
		row.Id = stmt.ColumnText(0)
		row.MarssId = stmt.ColumnText(1)
		row.GivenName = stmt.ColumnText(2)
		row.ChosenName = stmt.ColumnText(3)
		row.FamilyName = stmt.ColumnText(4)
		row.Email = stmt.ColumnText(5)
		row.Username = stmt.ColumnText(6)
		row.Grade = stmt.ColumnInt64(7)
		row.Homeroom = stmt.ColumnText(8)
		row.CaseManager = stmt.ColumnText(9)
		row.CreatedAt = stmt.ColumnText(10)
		row.UpdatedAt = stmt.ColumnText(11)
		isNullArchivedAt := stmt.ColumnIsNull(12)
		if !isNullArchivedAt {
			tmp := stmt.ColumnText(12)
			row.ArchivedAt = &tmp
		}
		res = append(res, row)
	}

	return res, nil
}

func OnceListStudentsByIepserviceType(
	tx *sqlite.Conn,
	serviceType string,
) (
	res []ListStudentsByIepserviceTypeRes,
	err error,
) {
	ps := ListStudentsByIepserviceType(tx)

	return ps.Run(
		serviceType,
	)
}
