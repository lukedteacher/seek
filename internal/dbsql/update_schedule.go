// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type UpdateScheduleParams struct {
	Title                    string `json:"title"`
	TeacherId                string `json:"teacher_id"`
	UpdatedAt                string `json:"updated_at"`
	LastEventCommitPosition  int64  `json:"last_event_commit_position"`
	LastEventPreparePosition int64  `json:"last_event_prepare_position"`
	Id                       string `json:"id"`
}

type UpdateScheduleStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func UpdateSchedule(tx *sqlite.Conn) *UpdateScheduleStmt {
	const querySQL = `
UPDATE schedules
SET title = ?1,
	teacher_id = ?2,
	updated_at = ?3,
	last_event_commit_position = ?4,
	last_event_prepare_position = ?5
WHERE id = ?6
    `

	ps := &UpdateScheduleStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *UpdateScheduleStmt) Run(
	params UpdateScheduleParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, params.Title)

	bindIndex++
	stmt.BindText(bindIndex, params.TeacherId)

	bindIndex++
	stmt.BindText(bindIndex, params.UpdatedAt)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++
	stmt.BindText(bindIndex, params.Id)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute updateschedule SQL: %w", err)
	}

	return nil
}

func OnceUpdateSchedule(
	tx *sqlite.Conn,
	params UpdateScheduleParams,
) (
	err error,
) {
	ps := UpdateSchedule(tx)

	return ps.Run(
		params,
	)
}
