// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type CreateHomeroomParams struct {
	Id                       string `json:"id"`
	Title                    string `json:"title"`
	GradesBitmask            int64  `json:"grades_bitmask"`
	LocationId               string `json:"location_id"`
	Image                    string `json:"image"`
	LastEventCommitPosition  int64  `json:"last_event_commit_position"`
	LastEventPreparePosition int64  `json:"last_event_prepare_position"`
	CreatedAt                string `json:"created_at"`
}

type CreateHomeroomStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func CreateHomeroom(tx *sqlite.Conn) *CreateHomeroomStmt {
	const querySQL = `
INSERT INTO homerooms (
	id, 
	title, 
	grades_bitmask, 
	location_id, 
	image, 
	last_event_commit_position, 
	last_event_prepare_position,
	created_at, 
	updated_at
)
VALUES (
	?1, 
	?2, 
	?3,
	?4,
	?5,
	?6, 
	?7,
	?8, 
	?8
)
ON CONFLICT (id) DO NOTHING
    `

	ps := &CreateHomeroomStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *CreateHomeroomStmt) Run(
	params CreateHomeroomParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, params.Id)

	bindIndex++
	stmt.BindText(bindIndex, params.Title)

	bindIndex++
	stmt.BindInt64(bindIndex, params.GradesBitmask)

	bindIndex++
	stmt.BindText(bindIndex, params.LocationId)

	bindIndex++
	stmt.BindText(bindIndex, params.Image)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++
	stmt.BindText(bindIndex, params.CreatedAt)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute createhomeroom SQL: %w", err)
	}

	return nil
}

func OnceCreateHomeroom(
	tx *sqlite.Conn,
	params CreateHomeroomParams,
) (
	err error,
) {
	ps := CreateHomeroom(tx)

	return ps.Run(
		params,
	)
}
