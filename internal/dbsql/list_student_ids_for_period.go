// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type ListStudentIdsForPeriodRes struct {
	PeriodId  string `json:"period_id"`
	StudentId string `json:"student_id"`
}

type ListStudentIdsForPeriodStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func ListStudentIdsForPeriod(tx *sqlite.Conn) *ListStudentIdsForPeriodStmt {
	const querySQL = `
SELECT period_id, student_id
FROM periods_students
WHERE period_id = ?1
ORDER BY period_id DESC
    `

	ps := &ListStudentIdsForPeriodStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *ListStudentIdsForPeriodStmt) Run(
	periodId string,
) (
	res []ListStudentIdsForPeriodRes,
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, periodId)

	bindIndex++

	// Execute the query
	for {
		if hasRow, err := stmt.Step(); err != nil {
			return res, fmt.Errorf("failed to execute {{.Name.Lower}} SQL: %w", err)
		} else if !hasRow {
			break
		}

		row := ListStudentIdsForPeriodRes{}
		row.PeriodId = stmt.ColumnText(0)
		row.StudentId = stmt.ColumnText(1)
		res = append(res, row)
	}

	return res, nil
}

func OnceListStudentIdsForPeriod(
	tx *sqlite.Conn,
	periodId string,
) (
	res []ListStudentIdsForPeriodRes,
	err error,
) {
	ps := ListStudentIdsForPeriod(tx)

	return ps.Run(
		periodId,
	)
}
