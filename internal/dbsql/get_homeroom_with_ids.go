// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type GetHomeroomWithIdsRes struct {
	Id          string `json:"id"`
	Title       string `json:"title"`
	LocationId  string `json:"location_id"`
	CreatedAt   string `json:"created_at"`
	UpdatedAt   string `json:"updated_at"`
	EducatorIds string `json:"educator_ids"`
	StudentIds  string `json:"student_ids"`
}

type GetHomeroomWithIdsStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func GetHomeroomWithIds(tx *sqlite.Conn) *GetHomeroomWithIdsStmt {
	const querySQL = `
SELECT
	h.id,
	h.title,
	h.location_id,
	h.created_at,
	h.updated_at,
	CAST(
		COALESCE(
			(SELECT json_group_array(he.educator_id) FROM homerooms_educators he WHERE he.homeroom_id = h.id),
			'[]'
		) AS TEXT
	) AS educator_ids,
	CAST(
		COALESCE(
			(SELECT json_group_array(hs.student_id) FROM homerooms_students hs WHERE hs.homeroom_id = h.id),
			'[]'
		) AS TEXT
	) AS student_ids
FROM homerooms h
WHERE h.id = ?1
    `

	ps := &GetHomeroomWithIdsStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *GetHomeroomWithIdsStmt) Run(
	id string,
) (
	res *GetHomeroomWithIdsRes,
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, id)

	bindIndex++

	// Execute the query
	if hasRow, err := stmt.Step(); err != nil {
		return res, fmt.Errorf("failed to execute {{.Name.Lower}} SQL: %w", err)
	} else if hasRow {
		row := GetHomeroomWithIdsRes{}
		row.Id = stmt.ColumnText(0)
		row.Title = stmt.ColumnText(1)
		row.LocationId = stmt.ColumnText(2)
		row.CreatedAt = stmt.ColumnText(3)
		row.UpdatedAt = stmt.ColumnText(4)
		row.EducatorIds = stmt.ColumnText(5)
		row.StudentIds = stmt.ColumnText(6)
		res = &row
	}

	return res, nil
}

func OnceGetHomeroomWithIds(
	tx *sqlite.Conn,
	id string,
) (
	res *GetHomeroomWithIdsRes,
	err error,
) {
	ps := GetHomeroomWithIds(tx)

	return ps.Run(
		id,
	)
}
