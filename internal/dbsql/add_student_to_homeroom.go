// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type AddStudentToHomeroomParams struct {
	HomeroomId               string `json:"homeroom_id"`
	LastEventCommitPosition  int64  `json:"last_event_commit_position"`
	LastEventPreparePosition int64  `json:"last_event_prepare_position"`
	UpdatedAt                string `json:"updated_at"`
	StudentId                string `json:"student_id"`
}

type AddStudentToHomeroomStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func AddStudentToHomeroom(tx *sqlite.Conn) *AddStudentToHomeroomStmt {
	const querySQL = `
UPDATE students
SET
	homeroom_id = ?1,
	last_event_commit_position = ?2,
	last_event_prepare_position = ?3,
	updated_at = ?4
WHERE id = ?5
    `

	ps := &AddStudentToHomeroomStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *AddStudentToHomeroomStmt) Run(
	params AddStudentToHomeroomParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, params.HomeroomId)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++
	stmt.BindText(bindIndex, params.UpdatedAt)

	bindIndex++
	stmt.BindText(bindIndex, params.StudentId)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute addstudenttohomeroom SQL: %w", err)
	}

	return nil
}

func OnceAddStudentToHomeroom(
	tx *sqlite.Conn,
	params AddStudentToHomeroomParams,
) (
	err error,
) {
	ps := AddStudentToHomeroom(tx)

	return ps.Run(
		params,
	)
}
