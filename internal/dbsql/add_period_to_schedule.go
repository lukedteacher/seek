// Code generated by "sqlc-gen-zombiezen". DO NOT EDIT.

package dbsql

import (
	"fmt"
	"zombiezen.com/go/sqlite"
)

type AddPeriodToScheduleParams struct {
	ScheduleId               string `json:"schedule_id"`
	PeriodId                 string `json:"period_id"`
	CreatedAt                string `json:"created_at"`
	LastEventCommitPosition  int64  `json:"last_event_commit_position"`
	LastEventPreparePosition int64  `json:"last_event_prepare_position"`
}

type AddPeriodToScheduleStmt struct {
	conn      *sqlite.Conn
	stmt      *sqlite.Stmt
	querySQL  string
	hasSlices bool
}

func AddPeriodToSchedule(tx *sqlite.Conn) *AddPeriodToScheduleStmt {
	const querySQL = `
INSERT INTO schedule_periods (schedule_id, period_id, created_at, updated_at, last_event_commit_position, last_event_prepare_position)
VALUES (?1, ?2, ?3, ?3, ?4, ?5)
ON CONFLICT (schedule_id, period_id) DO NOTHING
    `

	ps := &AddPeriodToScheduleStmt{
		conn:      tx,
		querySQL:  querySQL,
		hasSlices: false,
	}

	if !ps.hasSlices {
		ps.stmt = tx.Prep(querySQL)
	}

	return ps
}

func (ps *AddPeriodToScheduleStmt) Run(
	params AddPeriodToScheduleParams,
) (
	err error,
) {
	querySQL := ps.querySQL

	var stmt *sqlite.Stmt
	if ps.hasSlices {
		stmt = ps.conn.Prep(querySQL)
	} else {
		stmt = ps.stmt
	}

	defer func() {
		_ = stmt.ClearBindings()
		_ = stmt.Reset()
	}()

	bindIndex := 1
	// Bind parameters
	stmt.BindText(bindIndex, params.ScheduleId)

	bindIndex++
	stmt.BindText(bindIndex, params.PeriodId)

	bindIndex++
	stmt.BindText(bindIndex, params.CreatedAt)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventCommitPosition)

	bindIndex++
	stmt.BindInt64(bindIndex, params.LastEventPreparePosition)

	bindIndex++

	// Execute the query
	if _, err := stmt.Step(); err != nil {
		return fmt.Errorf("failed to execute addperiodtoschedule SQL: %w", err)
	}

	return nil
}

func OnceAddPeriodToSchedule(
	tx *sqlite.Conn,
	params AddPeriodToScheduleParams,
) (
	err error,
) {
	ps := AddPeriodToSchedule(tx)

	return ps.Run(
		params,
	)
}
