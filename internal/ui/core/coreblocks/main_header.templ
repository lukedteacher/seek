package coreblocks

import "seek/pkg/templui/components/separator"

templ HeaderMain(title, path string) {
	<header class="w-full md:px-3 display flex flex-col gap-3 mb-4">
		@Breadcrumbs(path)
		@separator.Separator(separator.Props{
			Class: "mb-3",
		})
		<h2>{ title }</h2>
	</header>
}
