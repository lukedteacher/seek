package coreblocks

import "seek/pkg/templui/components/separator"

templ HeaderMain(title, path string) {
	<header class="w-full md:px-3 display flex flex-col gap-3">
		@Breadcrumbs(path)
		@separator.Separator()
		<h2>{ title }</h2>
	</header>
}
