package pages

import (
	"seek/internal/features/users/models"
	"seek/internal/ui/core/corelayouts"
)

templ NotFound(user models.User) {
	@corelayouts.LayoutBase("SEEK: educator not found", user) {
		<main id="main">
			<section>
				<h3>not found</h3>
				<p>that educator doesn't seem to exist. are you sure you're in the right place?</p>
			</section>
		</main>
	}
}
