package blocks

import (
	"seek/internal/features/educators/dto"
	"seek/internal/views/blocks/forms"

	"github.com/starfederation/datastar-go/datastar"
)

templ EducatorEditForm(url string, view dto.EducatorView) {
	<form
		id="educator-edit-form"
		class="seek-form"
		data-on:submit={ datastar.PostSSE(url) }
	>
		<hgroup>
			<h2>edit educator</h2>
			<p>edit the data of an existing educator.</p>
		</hgroup>
		@FormSections(url, view)
		<footer>
			@forms.CancelButton()
			@forms.SaveButton()
		</footer>
	</form>
}
