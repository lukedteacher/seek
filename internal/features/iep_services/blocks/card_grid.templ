package blocks

import (
	"seek/internal/features/iep_services/dto"
)

templ CardGrid(views []dto.IEPServiceView) {
	<div class="service-minutes-card-grid">
		for i := range views {
			@Card(views[i])
		}
	</div>
}