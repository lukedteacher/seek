package pages

import (
	"seek/internal/domain/models"
	"seek/internal/features/iep_services/blocks"
	"seek/internal/features/iep_services/dto"
	"seek/internal/views"
	"seek/internal/views/components/icon"
	"seek/internal/views/layouts"
)



templ List(user models.User, view int, periods []dto.IEPServiceView) {
	@layouts.Base("SEEK: periods", user) {
		<main data-signals:view={ view } data-init={ views.LongRunningGetSSE("/iepservices/list/stream") }>
			<section>
				<h2>PERIODS</h2>
				<p>review and enter period data here</p>
				<p>many features to come</p>
				<div>
					<button data-on:click="$view=0; @get('/iepservices/list')">
						@icon.Icon("solar:list-bold-duotone")
						<span>list</span>
					</button>
					<button data-on:click="$view=1; @get('/iepservices/list')">
						@icon.Icon("solar:widget-bold-duotone")
						<span>cards</span>
					</button>
				</div>
				if view == 0 {
					@blocks.List(periods)
				} else {
					@blocks.CardGrid(periods)
				}
			</section>
		</main>
	}
}