package pages

import (
	"seek/internal/features/_shared/shareddto"
	"seek/internal/ui/core/coreblocks"
	"seek/internal/ui/core/coreblocks/tables"
	"seek/internal/ui/core/corelayouts"
	"seek/pkg/sse"
)

templ List(view shareddto.TableView) {
	@corelayouts.LayoutBase("SEEK: homerooms") {
		<main class="flex flex-col gap-4" data-init={ sse.LongRunningGetSSE("/homerooms/stream") }>
			@coreblocks.HeaderMain("all homerooms")
			@tables.DataTable(view)
		</main>
	}
}
