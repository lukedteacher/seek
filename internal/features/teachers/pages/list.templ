package pages

import (
	"seek/internal/domain/models"
	"seek/internal/features/teachers/blocks"
	"seek/internal/views"
	"seek/internal/views/components/icon"
	"seek/internal/views/layouts"
)

templ List(user models.User, view int64, teachers []models.Teacher) {
	@layouts.Base("SEEK: teachers", user) {
		<main data-signals:view={ view } data-init={ views.LongRunningGetSSE("/teachers/stream") }>
			<section>
				<h1>TEACHERS</h1>
				<p>review and enter teacher data here</p>
				<p>many features to come</p>
				<div>
					<button data-on:click="$view=0; @get('/teachers')">
						@icon.Icon("solar:list-bold-duotone")
						<span>list</span>
					</button>
					<button data-on:click="$view=1; @get('/teachers')">
						@icon.Icon("solar:widget-bold-duotone")
						<span>cards</span>
					</button>
				</div>
				if view == 0 {
					@blocks.List(teachers)
				} else {
					@blocks.Cards(teachers)
				}
			</section>
		</main>
	}
}