package pages

import (
	"fmt"

	"seek/internal/features/_shared/sharedmodels"
	"seek/internal/features/periods/blocks"
	"seek/internal/features/periods/dto"
	scheduleBlocks "seek/internal/features/schedules/blocks"
	scheduleDTO "seek/internal/features/schedules/dto"
	"seek/internal/ui/core/coreblocks"
	"seek/internal/ui/core/corelayouts"
	"seek/pkg/sse"
)

templ Edit(view dto.PeriodFormView, schedules []scheduleDTO.PersonWithScheduleView) {
	{{ url := sharedmodels.GetURLFromContext(ctx) }}
	@corelayouts.LayoutBase("SEEK: edit period") {
		<main data-init={ sse.LongRunningGetSSE(fmt.Sprintf("%s/stream", url)) }>
			@coreblocks.HeaderMain("edit period")
			<section class="w-full flex flex-col md:flex-row gap-4 h-full">
				@blocks.PeriodForm(view, schedules)
				@scheduleBlocks.ScheduleComponent(schedules)
			</section>
		</main>
	}
}
