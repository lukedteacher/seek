package pages

import (
	"fmt"

	"seek/internal/features/periods/blocks"
	"seek/internal/features/periods/dto"
	"seek/internal/features/users/models"
	"seek/internal/ui/core/corelayouts"
	"seek/pkg/sse"
)

templ Edit(user models.User, view dto.PeriodFormView) {
	@corelayouts.LayoutBase("SEEK: edit period", user) {
		<main data-init={ sse.LongRunningGetSSE(fmt.Sprintf("/periods/%s/edit/stream", view.ID)) }>
			<section>
				@blocks.EditPeriodForm(view)
			</section>
		</main>
	}
}
