package period_card

import (
	"fmt"
	"seek/internal/domain/models"
)

type PeriodCardViewModel struct {
	Period models.PeriodSignals
}

templ PeriodCard(vm PeriodCardViewModel) {
	<div 
		class="period-card"
		style={fmt.Sprintf("grid-column: 2; grid-row: %s / span %d;", models.TimeToRow(vm.Period.StartTime, 479), vm.Period.Duration)}
	>
		<h3>{ vm.Period.Title }</h3>
	</div>
	@PeriodCardStyles()
}

templ PeriodCardStyles() {
	<style>
		.period-card {
			width: 100%;
			display: flex;
			flex-direction: column;
			align-items: center;
			justify-content: center;
			background-color: var(--gruvbox-orange-neutral-0);
			border-style: solid;
			border-color: var(--gruvbox-orange-neutral--1);
			border-radius: var(--border-radius-0);
			font-size: var(--font-size--2);
		}
	</style>
}