// templui component card - version: v0.99.0 installed by templui v0.99.0
package card

import ()

type Props struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type HeaderProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type TitleProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type DescriptionProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type ContentProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type FooterProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

templ Card(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		class="card"
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ CardStyles() {
	<style>
		div.card {
			display: flex;
			flex-direction: column;
			width: 100%;
			border-radius: var(--border-radius-0);
			border-width: var(--border-width-0);
			border-style: solid;
			background-color: var(--neutral-6);
			color: var(--neutral-6-on);
			padding: var(--size-1);

			hgroup {
				display: flex;
				flex-direction: column;
				padding-bottom: var(--size-0);

				h3 {
					font-family: var(--font-industrial);
					font-size: var(--font-size-1);
					font-weight: var(--font-weight-medium);
					line-height: 1;
					letter-spacing: var(--font-letter-spacing--1);
				}

				p {
					font-size: var(--font-size--1);
					color: color-mix(in oklch, var(--neutral-6-on) 85%, black 15%);
				}
			}

			div {
				display: block;
				font-size: var(--font-size--1);
			}

			footer {
				display: flex;
				gap: var(--size-2);
				align-items: center;
				margin-top: auto;
				padding-top: var(--size-0);
			}
		}
	</style>
}

templ Header(props ...HeaderProps) {
	{{ var p HeaderProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<hgroup
		if p.ID != "" {
			id={ p.ID }
		}
		if p.Class != "" {
			class={ p.Class }
		}
		{ p.Attributes... }
	>
		{ children... }
	</hgroup>
}

templ Title(props ...TitleProps) {
	{{ var p TitleProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<h3
		if p.ID != "" {
			id={ p.ID }
		}
		if p.Class != "" {
			class={ p.Class }
		}
		{ p.Attributes... }
	>
		{ children... }
	</h3>
}

templ Description(props ...DescriptionProps) {
	{{ var p DescriptionProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<p
		if p.ID != "" {
			id={ p.ID }
		}
		if p.Class != "" {
			class={ p.Class }
		}
		{ p.Attributes... }
	>
		{ children... }
	</p>
}

templ Content(props ...ContentProps) {
	{{ var p ContentProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		if p.Class != "" {
			class={ p.Class }
		}
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Footer(props ...FooterProps) {
	{{ var p FooterProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<footer
		if p.ID != "" {
			id={ p.ID }
		}
		if p.Class != "" {
			class={ p.Class }
		}
		{ p.Attributes... }
	>
		{ children... }
	</footer>
}
