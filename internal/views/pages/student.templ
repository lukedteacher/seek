package pages

import (
	"fmt"

	"seek/internal/domain/models"
	"seek/internal/views"
	"seek/internal/views/layouts"
)

type StudentSignals struct {
	StudentDataView views.DataView
}

templ Student(student *models.Student) {
	@layouts.Base("SEEK: students") {
		<main data-init={ views.LongRunningGetSSE("/student/stream") }>
			<section>
				<h1>{ student.FirstLast() }</h1>
				<p>review and enter student data here</p>
				<p>many features to come</p>
				<div>
					<button data-on:click={ fmt.Sprintf("@delete('/student/%s')", student.Id) }>delete</button>
				</div>
			</section>
		</main>
	}
}